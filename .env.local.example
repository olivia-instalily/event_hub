# Copy to .env.local. Get the values from `supabase status` after `supabase start`.
# The anon key is the local default (safe to share); writes use the service-role key server-side.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<ANON_KEY from `supabase status`>
